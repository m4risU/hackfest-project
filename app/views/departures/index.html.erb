<table class="table table-striped table-bordered table-condensed">
  <thead>
  <tr>
    <th>Czas</th>
    <th>Akcje</th>
  </tr>
  </thead>
  <tbody>
  <% @departures.each do |departure| %>
    <tr>
      <td><%= departure.departing_at.strftime("%H:%M")  %></td>
      <td><%= link_to "Usuń", route_location_departure_path(@route, @location, departure), :method => :delete, :remote => true %></td>
    </tr>
  <% end %>
  </tbody>
</table>

